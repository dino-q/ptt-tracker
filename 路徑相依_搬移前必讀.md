# 路徑相依_搬移前必讀（PTT_Assistant）

> 這個資料夾若要搬移/改名，以下寫死絕對路徑的東西會斷，搬移後必須逐一修正。
> （建立日：2026-08-22）

## 1. Windows 排程 `PTT_Assistant_DailyCache`（最重要）

- 每日 08:30 無視窗執行，更新省錢版/熱門討論快取：
  `"...\PTT_Assistant\.venv\Scripts\pythonw.exe" "...\PTT_Assistant\server.py" --refresh-only`
- 搬移後修正方式：`schtasks /Delete /F /TN PTT_Assistant_DailyCache` 後照新路徑重建，
  並記得補 `Set-ScheduledTask -Settings (New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries)`（筆電用電池也要跑）。
- 驗證是否活著：看 `data\cache\*.json` 的 updated_at 有沒有每天前進，**不要只信 LastTaskResult**。

## 2. Claude 全域記憶 `project_ptt_assistant.md`

- `C:\Users\AG_Di\.claude\projects\C--Users-AG-Di\memory\project_ptt_assistant.md` 記了本資料夾絕對路徑，搬移後要更新。

## 3. 測試腳本引用外部共用 venv

- `tests\verify_*.py` 用 `C:\Users\AG_Di\Desktop\automation\Playwright\.venv\Scripts\python.exe` 執行（Playwright 集中安裝）。
  是「本資料夾引用外部」，搬移本資料夾不會斷；但 Playwright 資料夾搬移會斷這裡。

## 不會斷的部分

- `PTT工具.bat`、`server.py`、`web\`、`tracks.json`、`data\cache\` 全部用相對路徑（`%~dp0`、`__file__`），搬移後照常。

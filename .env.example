# 複製這個檔成 .env（同一個資料夾），填入金鑰即可，程式會自己讀。
# .env 已在 .gitignore，絕不會被推上 GitHub（這個 repo 是公開的）。
#
# 咖啡情報結構化 ＋ 省錢文章圖片辨識會用到。沒有這個檔也能跑，
# 只是那兩個功能會安靜跳過，其餘資料照常產生。
#
# GitHub Actions 那邊用 repo secret 注入同名變數，不吃這個檔；
# 已存在的環境變數優先，.env 只補不覆蓋。
GEMINI_API_KEY=貼上你的金鑰

# PTT 追蹤助手 使用說明

一套追蹤 PTT 的工具，分兩個版本：**線下版**（本機完整功能）與**線上版**（隨處可看的自動更新網頁）。

---

## 線上版（不用開電腦也會自己更新）

**網址：https://dino-q.github.io/ptt-tracker/** —— 手機、公司、任何裝置直接開。

- 內容：**省錢優惠**（分類頁籤瀏覽）＋**熱門文章**（三種排序），每篇可展開內文摘要、標「新」徽章
- 更新：GitHub 每天 **08:07～23:07 台灣時間每小時**自動爬一次（深夜停跑），你的電腦關機也照常更新
- 維護：改程式後 `git push` 上 GitHub 就自動重新部署，不用做任何其他事
- 線上版是唯讀的：不能掃自訂條件、不能下載 TXT——那些是線下版的事

## 線下版（完整功能）

### 怎麼啟動

**雙擊 `PTT工具.bat` → 按 Enter**（模式 1），會啟動本機服務並自動開瀏覽器到 http://127.0.0.1:8877 。關掉那個黑視窗＝關閉服務。

- 換新電腦或重灌後：先雙擊 `安裝套件.bat` 建好環境，再用 `PTT工具.bat`
- 進階（指令啟動）：`.venv\Scripts\python.exe server.py`（`--port 8878` 換埠、`--no-browser` 不開瀏覽器）
- bat 選模式 2 是舊版一句話 CLI（輸出 TXT），一般用不到

### 四個功能頁

| 功能頁 | 做什麼 |
|---|---|
| **省錢優惠** | 開頁直接顯示每日快取（分類：四大超商/超市量販/網購電商/餐飲美食/支付回饋），可重新掃描、一鍵掃「週末超商咖啡/飲料優惠」 |
| **熱門文章** | 全站人氣板高討論文章，排序：正在起飛（衝火速度）/總留言數/最新；填板名可看單一看板爆文 |
| **作者下載** | 輸入板名＋作者帳號 → 該作者全部（或篩標題關鍵字）文章依集數排序合併成一個 TXT，瀏覽器直接下載 |
| **進階掃描** | 任何看板＋自訂關鍵字條件掃描，條件可存成「追蹤項」一鍵重跑 |

通用能力：最上方**白話輸入**（例：「當前PTT的熱門討論」「幫我上PTT的某版找某作者把文章做成txt」）會自動切到對應功能並填好條件；任何結果清單都能「**下載全文 TXT**」（可勾含留言，最多 300 篇）。

### 線下版的自動更新

- Windows 排程 `PTT_Assistant_DailyCache` 每天 08:30 無視窗更新快取（除錯看 `data\refresh.log`）
- 服務開著時每 15 分鐘自檢，快取超過 6 小時自動補掃

## 兩版差異一覽

| | 線下版 | 線上版 |
|---|---|---|
| 省錢優惠／熱門文章瀏覽 | ✅ | ✅ |
| 內文摘要、分類、排序、「新」標記 | ✅ | ✅ |
| 作者下載、批次下載 TXT、進階掃描、追蹤項 | ✅ | ❌ |
| 自訂條件重新掃描 | ✅ | ❌（固定內建兩項） |
| 更新條件 | 電腦開機（排程）或服務開著 | 全自動，與你的電腦無關 |
| 誰能看 | 只有這台電腦 | 任何人（公開網頁） |

## 檔案與資料夾

| 位置 | 內容 |
|---|---|
| `PTT工具.bat` / `安裝套件.bat` | 啟動器／首次環境安裝（僅有的兩個 bat） |
| `output/` | 你下載的 TXT 都在這（**唯一會越積越多的地方**，可自行清理） |
| `data/cache/`、`tracks.json` | 快取與追蹤項（覆蓋式，不會長大） |
| `docs/CODE_MAP.md` | 程式碼地圖（改程式前先看） |
| `GIT_PUBLISH.md` | GitHub 推送規範（哪些絕不上 git） |
| `路徑相依_搬移前必讀.md` | 要搬移這個資料夾前必讀 |

## 常見問題

- **開 bat 說 port 被占用**：已經有一個服務在跑了，直接開 http://127.0.0.1:8877 即可
- **線上版資料沒更新**：看 repo 的 Actions 頁是否紅燈（紅燈＝爬取或資料異常被哨兵擋下，是保護機制）
- **想改線上更新頻率**：改 `.github/workflows/update.yml` 的 cron（UTC 時間）後 push
